import s from './SettingsTab.module.scss'

export function SettingsTab() {
   return (
      <div className={s.container}>
         <h2 className={s.heading}>Settings</h2>
         <p className={s.empty}>Configuration options coming soon.</p>
      </div>
   )
}
